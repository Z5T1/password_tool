# Main configuration file for password_tool
#
# To override these values on a per-user basis, create a file called
# .password_tool.conf in the user's home directory and set the values as
# desired in there.

# The remote server to connect to
SERVER=root@192.168.1.227

# The directory to use on the remote server. Can be absolute or relative to the
# user's home directory
REMOTE_DIR=password_tool

# The name of the window to type the commands in to
WINDOW_NAME="Remote Session Window"
